export class CheckoutClient{constructor(private readonly baseUrl:string){}async createSession(input:{productId:string;quantity:number},idempotencyKey=crypto.randomUUID()){const response=await fetch(`${this.baseUrl}/v1/checkout-sessions`,{method:'POST',headers:{'content-type':'application/json','idempotency-key':idempotencyKey},body:JSON.stringify(input)});const body=await response.json();if(!response.ok)throw Object.assign(new Error((body as{message?:string}).message??'Checkout failed'),{status:response.status});return body}async getSession(id:string,token:string){const response=await fetch(`${this.baseUrl}/v1/checkout-sessions/${encodeURIComponent(id)}`,{headers:{authorization:`Bearer ${token}`}});if(!response.ok)throw new Error('Unable to read checkout session');return response.json()}}
